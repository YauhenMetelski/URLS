!!!All commands needs to be run in specific folder where Docker-compose.yml file allocated.!!!!!!!!!!!!!!!!!
docker-compose up
docker-compose down
docker-compose --help
docker-compose up -d run in detach mode


